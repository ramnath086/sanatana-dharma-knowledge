# Taxonomy Discovery

Published classifications generate `/knowledge/:domain` pages. Category pages are derived from compiled public entities and never display drafts. Empty domains do not receive public routes in the generated sitemap.

Taxonomy is a discovery aid, not a universal hierarchy. Directed relationships remain the source of structural meaning.
