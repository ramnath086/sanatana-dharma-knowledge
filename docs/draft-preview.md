# Draft Preview

`/editorial/preview/:type/:id` renders an editorial entity with a prominent `UNPUBLISHED / EDITORIAL PREVIEW` marker, readiness checks, sources, claims, evidence, relationships and workflow actions.

Preview routes are not linked from public navigation. Drafts are excluded from public entities, search index and sitemap. This static development preview is not an authentication boundary; production preview access must be protected server-side.
