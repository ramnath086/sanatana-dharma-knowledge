# Relationship Navigation

Public entity pages display only relationships whose endpoints are both published. Each target becomes a navigable entity link. Editorial relationship records may include draft endpoints and internal notes, but those remain outside public generated relationship data.
